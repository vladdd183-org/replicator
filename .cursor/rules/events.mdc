---
description: Rules for Domain Events in Hyper-Porto
globs: ["**/Events.py", "**/events.py", "**/Listeners.py"]
---

# Domain Events Rules for Hyper-Porto

## Event Definition

```python
# Events.py
from src.Ship.Parents.Event import DomainEvent

class UserCreated(DomainEvent):
    """Emitted when a new user is created.
    
    Listeners:
    - on_user_created_send_welcome_email
    - on_user_created_init_settings
    """
    user_id: UUID
    email: str
    name: str
```

## Event Properties

1. **Immutable** — Events use `frozen=True` (inherited)
2. **Past tense** — `UserCreated`, not `CreateUser`
3. **Include IDs** — Pass IDs, not full entities
4. **Document listeners** — List in docstring

## Publishing Events

```python
# In Action
async with self.uow:
    user = AppUser(...)
    await self.uow.users.add(user)
    
    # Add event BEFORE commit
    self.uow.add_event(UserCreated(
        user_id=user.id,
        email=user.email,
        name=user.name,
    ))
    
    # Events emit AFTER commit
    await self.uow.commit()
```

## Listener Definition

```python
# Listeners.py
from litestar.events import listener

@listener("UserCreated")
async def on_user_created_send_welcome_email(
    user_id: str,
    email: str,
    name: str,
    app: Litestar | None = None,
    **kwargs,
) -> None:
    """Send welcome email to new user."""
    # Handle event - parameters come from event.model_dump()
    pass

@listener("UserCreated")
async def on_user_created_init_settings(
    user_id: str,
    email: str,
    **kwargs,
) -> None:
    """Initialize default settings for new user."""
    pass
```

## Listener Naming

Pattern: `on_{event_snake_case}_{action}`

| Event | Listener |
|-------|----------|
| UserCreated | `on_user_created_send_email` |
| OrderPaid | `on_order_paid_notify_warehouse` |

## Registration

```python
# App.py
from src.Containers.AppSection.UserModule.Listeners import (
    on_user_created_send_welcome_email,
    on_user_created_init_settings,
)

app = Litestar(
    ...,
    listeners=[
        on_user_created_send_welcome_email,
        on_user_created_init_settings,
    ],
)
```

## Event Emission (from UoW)

```python
# BaseUnitOfWork emits events after commit
async def commit(self) -> None:
    await self.connection.commit()
    
    # Emit events via Litestar's EventEmitter
    for event in self._events:
        # Event name is class name (e.g., "UserCreated")
        await self._emit(event.__class__.__name__, **event.model_dump())
    
    self._events.clear()
```

## Cross-Module Communication

**CRITICAL:** Containers NEVER import each other!

```
❌ WRONG:
OrderModule imports UserModule.GetUserQuery

✅ CORRECT:
OrderModule emits OrderCreated
UserModule listens to OrderCreated
```

## Rules

1. **Add event before commit** — `uow.add_event()` then `uow.commit()`
2. **Events are fire-and-forget** — Don't rely on listener result
3. **Listeners are idempotent** — Handle duplicates gracefully
4. **No cross-module imports** — Use events for communication
5. **Document all listeners** — In event docstring
