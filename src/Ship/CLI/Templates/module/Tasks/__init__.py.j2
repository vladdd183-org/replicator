"""{{ module_name }} Tasks (atomic operations).

Tasks are atomic, reusable operations shared between Actions.
They hold business logic that can be reused across modules.
"""

# Example:
# from src.Containers.{{ section }}.{{ module_name }}.Tasks.SomeTask import SomeTask

__all__: list[str] = [
    # Add task exports here
]
